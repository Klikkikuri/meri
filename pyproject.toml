[tool.poetry]
name = "meri"
version = "0.1.0"
description = "Meri – Klikkikuri backend service for news title generation"
authors = [
	"Teemu Autto <teemu.a.autto@jyu.fi>",
	"Juho Tarkkanen <juho.tarkkanen@hotmail.fi>",
	"Taija Kolehmainen <taija.s.kolehmainen@jyu.fi>"
]
packages = [
    { include = "valas", from = "src/" },
	{ include = "meri", from = "src/" }
]
readme = "README.md"

[tool.black]
line-length = 120

[tool.isort]
profile = "black"
line_length = 120

[tool.poetry.dependencies]
python = "^3.12"
pydantic-settings = "^2.5.2"
python-dotenv = "^1.0.1"
openai = "^1.51.1"
Jinja2 = "^3.1.4"
structlog = "^24.4.0"
opentelemetry-api = "^1.27.0"
playwright = "^1.47.0"

[tool.poetry.group.dev.dependencies]
black = "^24.8.0"
ruff = "^0.6.9"
poetry = "1.8.3"
isort = "^5.13.2"
pre-commit = "^4.0.1"
bandit = "^1.7.10"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
