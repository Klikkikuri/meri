default_language_version:
  python: python3.12

repos:
- repo: https://github.com/pre-commit/pre-commit-hooks
  rev: v5.0.0
  hooks:
    - id: check-merge-conflict
    - id: check-yaml
    - id: check-json
      exclude: '.devcontainer/devcontainer.json'
    - id: check-toml
    - id: check-added-large-files
    - id: detect-private-key
    - id: check-ast
    - id: end-of-file-fixer
    - id: trailing-whitespace

# Security checks
- repo: https://github.com/PyCQA/bandit
  rev: 1.8.3
  hooks:
    - id: bandit
      args: ["--skip=B101"]  # Skip assert statement check

- repo: https://github.com/astral-sh/uv-pre-commit
  # uv version.
  rev: 0.6.17
  hooks:
    - id: uv-lock

- repo: https://github.com/astral-sh/ruff-pre-commit
  # Ruff version.
  rev: v0.14.14
  hooks:
    # Run the linter.
    - id: ruff-check

# Meri and Sulku each vendor their own `packages/niitti` submodule; keep the two pointers in step.
- repo: local
  hooks:
    - id: check-niitti-sync
      name: Niitti submodules point at the same commit
      entry: scripts/check-niitti-sync.sh
      language: script
      always_run: true
      pass_filenames: false
