[project]
name = "luotsi"
version = "0.1.0"
description = "User feedback and analytics for Meri"
readme = "README.md"
authors = [
    { name = "Teemu Autto", email = "teemu@rautakuu.org" }
]
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.10.5",
    "tenacity>=9.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
